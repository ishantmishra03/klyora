import '../globals.css'
export const metadata = {
  title: "Explore Products | Klyore",
  description: "Sign in or create your Klyora account",
};

export default function AuthLayout({ children }) {
  return (
    
        {children}
      
  );
}
